extends layout.pug

block content
    section#content
        h2= text 
            if hint
                p
                i Hint: #{hint}
            br
            a(href=`/cards/${id}?side=${sideToShow}`)= sideToShowDisplay
            br
            a(href=`/cards/`) Next Card